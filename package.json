{
  "name": "merma-a-musica",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "bun --filter \"*\" dev",
    "build": "bun --filter \"*\" build",
    "test": "bun test",
    "lint": "oxlint"
  },
  "devDependencies": {
    "typescript": "^6.0.0",
    "oxlint": "latest"
  }
}
