# Bun Docs — `guides/streams`

Conversão de ReadableStream / Node Readable para tipos diversos.

**12** página(s) nesta seção.

| Arquivo | Título | Descrição |
|---|---|---|
| [`node-readable-to-arraybuffer.md`](./node-readable-to-arraybuffer.md) | Convert a Node.js Readable to an ArrayBuffer | To convert a Node.js `Readable` stream to an `ArrayBuffer` in Bun, you can create a new `Response` object with the strea… |
| [`node-readable-to-blob.md`](./node-readable-to-blob.md) | Convert a Node.js Readable to a Blob | To convert a Node.js `Readable` stream to a [`Blob`](https://developer.mozilla.org/en-US/docs/Web/API/Blob) in Bun, you … |
| [`node-readable-to-json.md`](./node-readable-to-json.md) | Convert a Node.js Readable to JSON | To convert a Node.js `Readable` stream to a JSON object in Bun, you can create a new [`Response`](https://developer.mozi… |
| [`node-readable-to-string.md`](./node-readable-to-string.md) | Convert a Node.js Readable to a string | To convert a Node.js `Readable` stream to a string in Bun, you can create a new [`Response`](https://developer.mozilla.o… |
| [`node-readable-to-uint8array.md`](./node-readable-to-uint8array.md) | Convert a Node.js Readable to an Uint8Array | To convert a Node.js `Readable` stream to an `Uint8Array` in Bun, you can create a new `Response` object with the stream… |
| [`to-array.md`](./to-array.md) | Convert a ReadableStream to an array of chunks | Bun provides several convenience functions for reading the contents of a [`ReadableStream`](https://developer.mozilla.or… |
| [`to-arraybuffer.md`](./to-arraybuffer.md) | Convert a ReadableStream to an ArrayBuffer | Bun provides several convenience functions for reading the contents of a [`ReadableStream`](https://developer.mozilla.or… |
| [`to-blob.md`](./to-blob.md) | Convert a ReadableStream to a Blob | Bun provides several convenience functions for reading the contents of a [`ReadableStream`](https://developer.mozilla.or… |
| [`to-buffer.md`](./to-buffer.md) | Convert a ReadableStream to a Buffer | Bun provides several convenience functions for reading the contents of a [`ReadableStream`](https://developer.mozilla.or… |
| [`to-json.md`](./to-json.md) | Convert a ReadableStream to JSON | Bun provides several convenience functions for reading the contents of a [`ReadableStream`](https://developer.mozilla.or… |
| [`to-string.md`](./to-string.md) | Convert a ReadableStream to a string | Bun provides several convenience functions for reading the contents of a [`ReadableStream`](https://developer.mozilla.or… |
| [`to-typedarray.md`](./to-typedarray.md) | Convert a ReadableStream to a Uint8Array | Bun provides several convenience functions for reading the contents of a [`ReadableStream`](https://developer.mozilla.or… |
