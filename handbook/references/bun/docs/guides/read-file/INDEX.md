# Bun Docs — `guides/read-file`

Leitura de arquivos em vários formatos.

**9** página(s) nesta seção.

| Arquivo | Título | Descrição |
|---|---|---|
| [`arraybuffer.md`](./arraybuffer.md) | Read a file to an ArrayBuffer | The `Bun.file()` function accepts a path and returns a `BunFile` instance. The `BunFile` class extends `Blob` and allows… |
| [`buffer.md`](./buffer.md) | Read a file to a Buffer | The `Bun.file()` function accepts a path and returns a `BunFile` instance. The `BunFile` class extends `Blob` and allows… |
| [`exists.md`](./exists.md) | Check if a file exists | The `Bun.file()` function accepts a path and returns a `BunFile` instance. Use the `.exists()` method to check if a file… |
| [`json.md`](./json.md) | Read a JSON file | The `Bun.file()` function accepts a path and returns a `BunFile` instance. The `BunFile` class extends `Blob` and allows… |
| [`mime.md`](./mime.md) | Get the MIME type of a file | The `Bun.file()` function accepts a path and returns a `BunFile` instance. The `BunFile` class extends `Blob`, so use th… |
| [`stream.md`](./stream.md) | Read a file as a ReadableStream | The `Bun.file()` function accepts a path and returns a `BunFile` instance. The `BunFile` class extends `Blob` and allows… |
| [`string.md`](./string.md) | Read a file as a string | The `Bun.file()` function accepts a path and returns a `BunFile` instance. The `BunFile` class extends `Blob` and allows… |
| [`uint8array.md`](./uint8array.md) | Read a file to a Uint8Array | The `Bun.file()` function accepts a path and returns a `BunFile` instance. The `BunFile` class extends `Blob` and allows… |
| [`watch.md`](./watch.md) | Watch a directory for changes | Bun implements the `node:fs` module, including the `fs.watch` function for listening for file system changes. |
